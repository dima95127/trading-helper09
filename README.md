# Trading Helper

Android app built with Kivy + Buildozer + GitHub Actions.

## Build

Push to `main` branch — GitHub Actions will build APK automatically.

## Download

Go to Actions tab → click latest run → download `trading-helper-apk` artifact.
